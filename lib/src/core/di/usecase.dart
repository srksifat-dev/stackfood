part of "injection_container.dart";

Future<void> _initUsecase() async {
  // sl.registerSingleton<GetMovieUseCase>(GetMovieUseCase(movieRepository: sl<MovieRepository>()));
  // sl.registerSingleton<SearchMovieUseCase>(SearchMovieUseCase(movieRepository: sl<MovieRepository>()));
}
